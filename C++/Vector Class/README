
To compile first create the bin folder for the executable

    mkdir -p bin

Then use the Makefile to compile the program by using the following command

    make all

Finally run the executable by using the following command

    ./bin/main
